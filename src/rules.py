# rules.py - mirror of data/rules.json for quick importing (optional)
from data.rules import RULES  # if you convert JSON to Python module

# OR keep simple mapping
# This file is optional. The application loads rules from data/rules.json by default.
